#include <stdio.h>
#include <stdlib.h>
#include <math.h> 

int main(int argc, char *argv[]) {
    int x1,x2,y1,y2;
    scanf("%d%d", &x1, &y1);
    scanf("%d%d", &x2, &y2);
    double ans = sqrt((x2 - x1) * (x2 - x1) + (y2 - y1) * (y2 - y1));
    printf("%.2f\n",ans);
	return 0;
}
