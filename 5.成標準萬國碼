#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {
    wchar_t unicode;
    while (wcin >> unicode) {
        unsigned int ans = static_cast<unsigned int>(unicode);//字元符號轉換成標準萬國碼
        wcout << ans << endl;
    }
    return 0;
}
