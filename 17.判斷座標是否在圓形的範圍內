#include <iostream>
#include <cmath>
using namespace std;

int main(int argc, char *argv[]) {
    int x, y;
    const int radius = 100;
    while (cin >> x >> y) {
        double distance = sqrt(x * x + y * y); //√(x² + y²)
        if (distance <= radius) {
            cout << "inside" << endl;
        } else {
            cout << "outside" << endl;
        }
    }
	return 0;
}
