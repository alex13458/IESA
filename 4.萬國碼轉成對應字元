#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {
    unsigned int unicode;
    while (cin >> unicode) {
        wchar_t wc = static_cast<wchar_t>(unicode); //萬國碼轉成對應字元
        wcout << wc << endl;
    }
    return 0;
}
