#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    int month;
    while(scanf("%d",&month)!=EOF){
        if(month>2 && month<=5 ){
            printf("Spring\n");
        }else if (month>5 && month<=8){
            printf("Summer\n");
        }else if(month>8 && month<12){
            printf("Autumn\n");
        }else{
            printf("Winter\n");
        }
    }
	return 0;
}
