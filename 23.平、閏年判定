#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {
    int year;
    cin >>year;
    if ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)) {
        cout<<"Bissextile Year\n";//輸出閏年
    } else {
        cout<<"Common Year\n"; //輸出平年
    }
	return 0;
}
