#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[]) {
    int n;
    scanf("%d", &n);  
    for (int i = 0; i < n; i++) {
        char goo[257];  
        scanf("%s", goo);  
        int length = strlen(goo);  // 計算字串長度
        for (int j = length - 1; j >= 0; j--) { //// 反轉輸出字串
            printf("%c", goo[j]);
        }
        printf("\n"); 
    }
	return 0;
}
