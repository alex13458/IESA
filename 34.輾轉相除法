#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {
    int a, b;
    cin >> a >> b;
    while (b != 0) { //輾轉相除法
        int temp = b;
        b = a % b;
        a = temp;
    }
    cout <<a << endl;
	return 0;
}
