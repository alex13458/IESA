#include <iostream>
#include <vector>
using namespace std;

int main(int argc, char *argv[]) {
    int n;
    cin >> n;
    vector<int> score(n);

    for (int i = 0; i < n; i++) {
        cin >> score[i];
    }

    // Insertion Sort
    for (int i = 1; i < n; i++) {
        int key = score[i];
        int j = i - 1;
        while (j >= 0 && score[j] > key) {
            score[j + 1] = score[j];
            j--;
        }
        score[j + 1] = key;
    }

    for (int i = 0; i < n; i++) {
        cout << score[i] << '\n';
    }

    return 0;
}
