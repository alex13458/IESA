#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {
    int a,b,c;
    while(cin>>a>>b>>c){
        int max = a;
        if (b > max) {
            max = b;
        }
        if (c > max) {
            max = c;
        }
    cout << max<<endl;
    }
	return 0;
}
