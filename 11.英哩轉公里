#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    int miles;
    double kilometers;
    while(scanf("%d", &miles)!=EOF){
    kilometers = miles * 1.6;
    printf("%.1f\n", kilometers);
}
	return 0;
}
