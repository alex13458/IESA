#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    int miles;
    double kilometers;
    while(scanf("%d", &miles)!=EOF){
    kilometers = miles * 1.6;  //1 英哩= 1.6 公里
    printf("%.1f\n", kilometers); //取到小數點以下第一位
}
	return 0;
}
