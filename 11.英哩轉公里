#include <iostream>
using namespace std;

int main() {
    int miles;
    double kilometers;
    while (cin >> miles) {
        kilometers = miles * 1.6;//1 英哩= 1.6 公里
        cout << fixed;
        cout.precision(1); //取到小數點以下第一位
        cout << kilometers << endl;
    }
    return 0;
}
