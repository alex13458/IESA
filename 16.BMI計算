#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    int w,h;
    double BMI;
    while(scanf("%d%d",&w,&h)!=EOF){
        double height= h / 100.0;
        BMI=w / (height*height);
        printf("%.2f",BMI);
    }
	return 0;
}
